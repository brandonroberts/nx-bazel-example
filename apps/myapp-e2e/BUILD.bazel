# ==== Generated by Nx
# ==== Do not touch

load("@npm//@nrwl/cli:index.bzl", "nx", "nx_test")

filegroup(
    name = "myapp-e2e",
    srcs = glob(["**"]) + [
        # Dependencies
        "//apps/myapp:myapp",
        
    ],
    visibility = ["//visibility:public"],
)


nx_test(
    name = "e2e",
    args = [
        "run",
        "myapp-e2e:e2e",
    ],
    data = [
        ":myapp-e2e",
        # Root Files
        "//:root-files",
        # Node Modules
        "@npm//:node_modules"
    ],
)

nx_test(
    name = "e2e__production",
    args = [
        "run",
        "myapp-e2e:e2e:production",
    ],
    data = [
        ":myapp-e2e",
        # Root Files
        "//:root-files",
        # Node Modules
        "@npm//:node_modules"
    ],
)

nx_test(
    name = "lint",
    args = [
        "run",
        "myapp-e2e:lint",
    ],
    data = [
        ":myapp-e2e",
        # Root Files
        "//:root-files",
        # Node Modules
        "@npm//:node_modules"
    ],
)

